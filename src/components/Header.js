import React from "react";

const Header = () => {
  return (
    <header id="head">
      <div className="logo">
        <span>Books-API</span>
      </div>
    </header>
  );
};

export default Header;
